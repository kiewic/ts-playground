{
    "compilerOptions": {
        "target": "ES6", // needed to use native promises
        "lib": ["ES2015","DOM","ES2018.Promise"],
        "outDir": "./build/"
    },
    "include": [
        "src/**/*"
    ]
}
